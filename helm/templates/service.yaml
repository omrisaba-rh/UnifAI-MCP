apiVersion: v1
kind: Service
metadata:
  name: {{ include "unifai-mcp.fullname" . }}
  labels:
    {{- include "unifai-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "unifai-mcp.selectorLabels" . | nindent 4 }}
